# A Todo Tracker Web Application thst uses class features of ES6 to make modules

# For setup the app

run "npm install"

# For run the dev server

run 'npm run serve'

# For creating a build

run 'npm run build'
